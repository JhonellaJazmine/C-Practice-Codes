//Write a program in C to display the cube of the number up to given an integer.

#include <stdio.h>

int main()
{
    //declaration of variables
    int n,cube;

    printf("Input number of terms : ");  //prompts user to input number of terms
    scanf("%d",&n);                     //gets the nth terms input by the user

    for(int i=0;i<n;i++)                                    //loop for printing cube of 1 up to nth term
    {
        cube=(i+1)*(i+1)*(i+1);
        printf("The cube of %d is %d\n",i+1,cube);     //prints the cube of i
    }

}
